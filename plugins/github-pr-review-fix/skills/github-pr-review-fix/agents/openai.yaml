interface:
  display_name: "PR Review + Fix"
  short_description: "Fast PR review, optional safe fixes"
  default_prompt: "Use $github-pr-review-fix to review these PRs. Findings first; no edits unless I ask:"
policy:
  allow_implicit_invocation: true
