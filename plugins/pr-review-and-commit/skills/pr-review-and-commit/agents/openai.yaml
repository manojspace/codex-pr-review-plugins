interface:
  display_name: "PR-Review and Commit"
  short_description: "Review, fix, push, and summarize linked PRs"
  default_prompt: "Use $pr-review-and-commit to review and fix these PRs, commit as ai review, push, and comment on each PR:"
policy:
  allow_implicit_invocation: false
